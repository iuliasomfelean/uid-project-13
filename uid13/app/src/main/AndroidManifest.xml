<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.uid13">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomepageActivity"
            android:label="@string/title_activity_homepage2"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".chat.ChatActivity" />
        <activity
            android:name=".chat.ContactsActivity"
            android:label="@string/title_activity_contacts"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".chat.CreateContact" />
        <activity android:name=".EventsListActivity" />
        <activity android:name=".DetailedEventActivity" />
        <activity android:name=".CreateEventActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".GroupAtivity" />
        <activity android:name=".GroupDetails"></activity>

        <activity android:name=".SearchedUserActivity" />

        <activity
            android:name=".CommentActivity"
            android:label="Comment"
            android:theme="@style/AppTheme.NoActionBar"></activity>


    </application>

</manifest>